/**
 * Table Extensions
 * 
 * This directory contains extension configurations for tables that customize
 * search behavior including search weights, column aliases, and validators.
 * 
 * Each file should export a TableExtension as the default export.
 * Extensions are automatically loaded at startup.
 */

export {postsExtension} from './posts'
